# 贡献与维护约定

## 变更类型

- `docs`：学习资料、架构、决策和说明。
- `design`：需求、工具契约、数据格式和评测口径。
- `feat`：未来的功能实现。
- `fix`：未来的问题修复。
- `security`：密钥、权限、日志或依赖安全调整。

## 推荐流程

1. 从一个边界明确的小任务开始，先写清验收标准。
2. 使用短分支开发，建议名称如 `docs/tool-contracts` 或 `feat/handwritten-loop`。
3. 一次提交只表达一个完整意图，提交信息不得包含密钥和敏感日志。
4. 提交前检查文档链接、评测影响、暂存文件列表和密钥排除状态。
5. 通过 Pull Request 说明“为什么改、改了什么、如何验证、有什么风险”。

## 设计变更

出现以下情况时，在 `FoodAssistant/docs/decisions/` 新增一条 ADR：

- 更换核心框架或供应商策略；
- 修改共享工具契约或数据格式；
- 引入会产生费用或真实副作用的工具；
- 改变安全边界、日志策略或评测标准。

## Pull Request 检查表

- [ ] 没有真实密钥、个人数据、完整模型日志或本地运行产物。
- [ ] 三个分支仍共享相同的需求、契约和评测口径。
- [ ] 行为变化有对应评测用例或明确说明。
- [ ] 新增依赖有必要性、版本和许可证说明。
- [ ] 面向初学者的文档能独立说明如何理解和验证变更。
