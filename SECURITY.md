# 安全与密钥管理

## 本仓库的密钥边界

现有 `APIKEY/` 目录被视为本地密钥保管区。本次整理不读取、不移动、不改写其中任何文件，并通过根目录 `.gitignore` 将该目录排除在版本控制之外。

推荐的长期做法是把密钥放入操作系统环境变量、受控的本地 `.env` 或专业密钥管理服务。仓库只保存 `.env.example`，且所有值为空。

## 禁止事项

- 不把真实密钥复制到代码、文档、Issue、PR、截图、聊天记录或日志。
- 不把完整请求头、模型请求、工具结果直接写入公开日志。
- 不因“文件已删除”就认为泄漏已经消失；Git 历史和远端副本仍可能保留内容。
- 不允许 Agent 自行开启付费模型回退或扩大工具权限。

## Agent 特有风险

| 风险 | 基线控制 |
|---|---|
| Prompt Injection | 外部内容只作为数据；系统规则和工具权限不能由检索内容改写 |
| 越权工具调用 | 工具采用最小权限；有副作用的动作要求明确确认 |
| 敏感信息进入上下文 | 输入模型前做字段筛选；不发送密钥、Cookie 和无关个人数据 |
| 日志泄漏 | 默认不记录完整 Prompt、响应和工具载荷；必要时脱敏并限制保留时间 |
| 无限循环与费用失控 | 限制最大轮次、超时、Token 和预算；付费回退默认关闭 |
| 不可信工具结果 | 校验数据类型、大小和来源；错误按结构化方式返回 |

## 提交前检查

1. 确认 `git status --short --ignored` 中 `APIKEY/` 显示为忽略状态。
2. 只检查暂存区文件名，确认没有 `.env`、密钥、日志或本地数据。
3. 对非敏感文件执行令牌特征扫描，扫描结果只输出文件名，不输出匹配内容。
4. 在 GitHub 仓库设置中启用 Secret Scanning 和 Push Protection（若账户与仓库类型支持）。

## 疑似泄漏响应

1. 立即撤销并轮换对应密钥。
2. 暂停推送和发布，确认泄漏范围。
3. 从当前文件和 Git 历史中清除敏感内容；改写历史属于高影响操作，应先备份并取得明确授权。
4. 检查供应商用量与审计日志，确认是否存在异常调用。
5. 记录原因并补充防复发检查。
